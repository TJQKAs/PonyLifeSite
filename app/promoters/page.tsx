import PlaceholderPage from "@/_components/PlaceholderPage";

export default function PromotersPage() {
  return (
    <PlaceholderPage
      title="Promoters"
      description="Select works from the catalog and promote them independently. No assignments, no guarantees."
    />
  );
}