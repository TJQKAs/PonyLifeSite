import PlaceholderPage from "@/app/_components/PlaceholderPage";

export default function CatalogPage() {
  return (
    <PlaceholderPage
      title="Catalog"
      description="Public works catalog (placeholder). This will become the core interactive section."
    />
  );
}